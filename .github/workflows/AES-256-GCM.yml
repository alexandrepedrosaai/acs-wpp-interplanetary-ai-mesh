name: AES-256-GCM CI/CD

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
    build:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout do código
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Configuração do ambiente
      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake openssl

      # 3. Compilação do binário com AES-256-GCM
      - name: Build AES binary
        run: |
          mkdir -p build
          cd build
          cmake ..
          make
          test -f aes-executable || (echo "Binary not found"; exit 1)

      # 4. Upload do artefato
      - name: Upload AES artifact
        uses: actions/upload-artifact@v4
        with:
          name: aes-binary
          path: build/aes-executable

    release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      # 5. Che allckout do código
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 6. Baixar artefato gerado
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: aes-binary
          path: ./dist

      # 7. Criar Release automático
      - name: Create AES Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./dist/aes-executable
          tag_name: aes-v1.0.${{ github.run_number }}
          name: AES-256-GCM Release v1.0.${{ github.run_number }}
          body: "Automated build of AES-256-GCM binary."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          continue-on-error:      
          concurrency: 
          cancel-timeout-minutes: 
      - name: Setup Node.js environment
        uses: actions/setup-node@v6.3.0
        concurrency: 
        cancel-timeout-minutes: 
        with:
    # Version Spec of the version to use. Examples: 12.x, 10.15.1, >=10.15.0.
        node-version: # optional
    # File containing the version Spec of the version to use.  Examples: package.json, .nvmrc, .node-version, .tool-versions.
        node-version-file: # optional
    # Target architecture for Node to use. Examples: x86, x64. Will use system architecture by default.
        architecture: # optional
    # Set this option if you want the action to check for the latest available version that satisfies the version spec.
        check-latest: # optional
    # Optional registry to set up for auth. Will set the registry in a project level .npmrc and .yarnrc file, and set up auth to read in from env.NODE_AUTH_TOKEN.
        registry-url: # optional
    # Optional scope for authenticating against scoped registries. Will fall back to the repository owner when using the GitHub Packages registry (https://npm.pkg.github.com/).
        scope: # optional
    # Used to pull node distributions from node-versions. Since there's a default, this is typically not supplied by the user. When running this action on github.com, the default value is sufficient. When running on GHES, you can pass a personal access token for github.com if you are experiencing rate limiting.
        token: # optional, default is ${{ github.server_url == 'https://github.com' && github.token || '' }}
    # Used to specify a package manager for caching in the default directory. Supported values: npm, yarn, pnpm.
        cache: # optional
    # Set to false to disable automatic caching. By default, caching is enabled when either devEngines.packageManager or the top-level packageManager field in package.json specifies npm as the package manager.
        package-manager-cache: # optional, default is true
    # Used to specify the path to a dependency file: package-lock.json, yarn.lock, etc. Supports wildcards or a list of file names for caching multiple dependencies.
        cache-dependency-path: # optional
    # Used to specify an alternative mirror to download Node.js binaries from
        mirror: # optional
    # The token used as Authorization header when fetching from the mirror
       mirror-token: # optional

